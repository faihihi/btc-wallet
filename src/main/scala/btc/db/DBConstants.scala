package btc.db

object DBConstants {
  val DATE_TIME  = "date_time"  // Transaction date and time in UTC 0
  val AMOUNT     = "amount"     // Transaction amount
  val CREATED_AT = "created_at" // Date and time that the transaction is stored in DB
  val DATE       = "date"       // Transaction date in UTC 0
  val HOUR       = "hour"       // Transaction hour in UTC 0
}
